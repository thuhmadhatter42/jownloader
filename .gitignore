# local working notes and session state — never part of the published project
CLAUDE.md
PICKUP.md
CHANGELOG.md
.remember/
archive/

# test profiles, downloads, logs
tests/test-run/

# extension signing key lives outside the repo
*.pem
.DS_Store

# python bytecode; the engine's per-Mac state (which browser held the session, update stamp, an optional cookies.txt)
__pycache__/
native/.cookie-source
native/.last-ytdlp-upgrade
native/cookies.txt
